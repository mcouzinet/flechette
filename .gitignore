node_modules
.env
.DS_Store
dist/

# Firebase emulator artifacts
*-debug.log
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# Sensitive — never commit
recovery-codes.txt

# Redundant root copies; the builds use the ones inside ios/App/App/ and android/app/
/GoogleService-Info.plist
/google-services.json

# Xcode build output (DerivedData) — machine-specific, regenerated on each build
/build/
